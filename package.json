{
  "name": "gymbros",
  "version": "1.0.0",
  "private": true,
  "description": "GymBros - Fitness tracking and analytics platform",
  "scripts": {
    "dev": "pnpm --filter web dev & pnpm --filter mobile dev & pnpm --filter api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

